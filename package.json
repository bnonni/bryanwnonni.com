{
  "name": "bryanwnonni.com",
  "version": "4.1.25",
  "engines": {
    "node": "16.x",
    "yarn": "^1.22.15"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-loading": "^2.0.3",
    "react-scripts": "^5.0.1",
    "smooth-scroll": "^16.1.3",
    "travis": "^0.1.1",
    "yarn": "^1.13.0"
  },
  "scripts": {
    "dev": "concurrently --kill-others yarn:dev:*",
    "dev:frontend": "PORT=8081 react-scripts start",
    "dev:backend": "cd dwn-server && npm run server",
    "setup": "yarn && cd dwn-server && npm i && cd .. && echo 'ready for build'",
    "build": "concurrently yarn:build:*",
    "build:frontend": "react-scripts build",
    "build:backend": "cd dwn-server && npm run compile",
    "start": "concurrently --kill-others yarn:prod:*",
    "prod:frontend": "PORT=8080 react-scripts start",
    "prod:backend": "cd dwn-server && npm run server",
    "predeploy": "yarn run build",
    "deploy": "echo TODO",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}