{
  "name": "bryan-w-nonni-portfolio",
  "version": "0.1.0",
  "dependencies": {
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5",
    "travis": "^0.1.1",
    "yarn": "^1.13.0"
  },
  "scripts": {
    "start": "PORT=8081 react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "predeploy": "yarn run build",
    "deploy": "gh pages -d build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
